/// Spacing and illustration sizes shared across Color Quiz screens.
abstract final class ColorQuizLayout {
  static const double scrollContentTopPadding = 0;
  /// Padding inside the white card (tight top so the image sits higher).
  static const double cardTopPadding = 6;
  static const double cardHorizontalPadding = 18;
  static const double cardBottomPadding = 22;
  static const double imageToPromptGap = 12;
  static const double promptToOptionsGap = 18;
}
